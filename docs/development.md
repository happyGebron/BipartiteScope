# Development

Use Python 3.11 or later. Install editable dependencies with `python -m pip install -e '.[train,api,dev]'`. Run `python -m unittest discover -s tests -v`. Keep additions label-free, sparse whenever possible, deterministic on ties, and accompanied by tests and docs.
