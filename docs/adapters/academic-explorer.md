# Academic explorer adapter

`AcademicExplorerAdapter` translates `{researcher_id,paper_id}` authorship records and researcher feature vectors into the canonical graph. Query members are related researchers; support entities expose the papers behind the connection.
