# Recommendation adapter

`RecommendationAdapter` translates `{user_id,item_id}` interactions plus user features to the canonical U/V graph. Query members are a cohort; V-side support entities are the items that characterize it. Business-specific ranking remains outside Core.
