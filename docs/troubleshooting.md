# Troubleshooting

If training is unavailable, install the `train` extra with a compatible PyTorch build. If a feature file is rejected, ensure every U ID referenced by an edge has one finite nonnegative row. If a query stops early, inspect its trace: an empty frontier and a failed structural gate are meaningful results, not silent errors.
