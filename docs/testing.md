# Testing

The suite verifies canonical schema validation, transition invariants, deterministic Top-k ties, W symmetry, snapshot round trips, online query behavior, and adapter boundaries. The product test criterion is mathematical and API behavior—not paper F1, NMI, Jaccard, or benchmark leaderboards.
